apiVersion: v1
kind: Service
metadata:
  name: api-movimentacoes
spec:
  selector:
    app: api-movimentacoes
  type: NodePort
  ports:
  - port: 80 #porta servico
    targetPort: 80 #porta no pod
    protocol: TCP
