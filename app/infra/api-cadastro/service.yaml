apiVersion: v1
kind: Service
metadata:
  name: api-cadastro
spec:
  selector:
    app: api-cadastro
  type: NodePort
  ports:
  - port: 80 #porta servico
    targetPort: 80 #porta no pod
    protocol: TCP
